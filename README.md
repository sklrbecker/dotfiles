# Skylar's Dotfiles

dotfiles for personal system

## Requirements

- git
- stow
- neovim

## Installation

Clone dotfiles repo into ~/.dotfiles/

`$ git clone # ~/.dotfiles/
$ cd ~/.dotfiles`

Use GNU stow to create symlinks

`$ stow .`
